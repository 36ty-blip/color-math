# Examples

The fixtures under `../tests/original/` are source notes; `../tests/expected/` contains the matching colorized output.

```bash
python -m color_math --file tests/original/derivatives.md
python -m color_math --self-test
```
